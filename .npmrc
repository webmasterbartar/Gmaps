# Skip Chromium download - we'll use system Chrome or download manually
# PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# Or specify custom download host (for Iran/filtered networks)
# You can uncomment this if download fails
# PUPPETEER_DOWNLOAD_HOST=https://registry.npmmirror.com/-/binary/chrome-for-testing
